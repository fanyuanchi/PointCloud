## 📁 Code Structure

The `PCPS-RBVH(Only)/` directory is organized into the following components:

- `core/`  
  Core implementation of PC-PS, including the worker logic and main processing pipeline.

- `index/`  
  Pure R-BVH implementations used in PC-PS.

- `util/`  
  Utility functions for time measurement.

